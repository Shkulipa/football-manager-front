import styles from './multiplayerMenu.module.scss';

export function MultiplayerMenu() {
	return (
		<div className={styles.multiplayerMenu}>
			Sorry, development is still in progress :(
		</div>
	);
}
